global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Clutch nodes push OTLP metrics to the collector, which re-exposes them here.
  - job_name: clutch
    metrics_path: /metrics
    static_configs:
      - targets:
          - otel-collector:8889
